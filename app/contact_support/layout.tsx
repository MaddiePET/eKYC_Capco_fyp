import React from "react";

export const metadata = {
  title: "Contact Support - DTCOB",
  description: "Contact Support page for DTCOB banking services.",
};

export default function ContactSupportLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}